server:
  port: 8086 # to be updated
spring:
  application:
    name: shipping-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:shippingservicedb
    driverClassName: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

